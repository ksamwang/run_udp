# UDPTunnelLAN 连接优化任务

目标：在服务端禁用中继转发时，提升 UDPTunnelLAN 的 P2P 连接成功率、首次连接速度和路径状态准确性，使常见业务连接体验尽量接近老 Agent。

## 第一阶段：连接状态和 P2P 收敛

- [ ] Relay 禁用时持续 P2P
  - [ ] 客户端识别服务端 relay 禁用状态。
  - [ ] relay 禁用时，P2P 30 秒快速打洞失败后不进入无效 `relay_udp` 死状态。
  - [ ] 增加 `long_punching` 状态，快速打洞结束后降频持续打洞。
  - [ ] 持续打洞生命周期绑定活跃业务会话，业务会话结束或超时后再退出。

- [ ] 修正 `p2p_datagram ready` 判定
  - [ ] 单边收到探测包只能标记为 `one_way_p2p`。
  - [ ] 只有双方完成数据面 ack，且确认同一个 peer / 地址 / 会话标识后，才标记 `p2p_ready`。
  - [ ] 避免 `.2` 认为 `.3` 已直连，而 `.3` 实际已经失败或进入 relay 的状态分裂。

- [ ] 降低 socket rotation 频率
  - [ ] relay 禁用时避免因 relay 打开失败触发频繁 socket rotation。
  - [ ] 活跃打洞会话期间优先保持 UDP socket 稳定。
  - [ ] 只有 socket 明确异常、长时间完全无入站、或公网映射确认失效时才 rotate。

## 第二阶段：会话感知打洞

- [ ] 从 Wintun 识别真实业务连接意图
  - [ ] 捕获 TCP SYN，识别源虚拟 IP、目标虚拟 IP、协议、目标端口、对端设备 ID。
  - [ ] 识别常见交互业务端口，例如 RDP `3389`、SSH `22`、SMB `445`。
  - [ ] 为每个新连接生成临时会话记录。

- [ ] 会话提升为高优先级打洞目标
  - [ ] 针对真实业务会话持续打洞，而不是只做泛 peer 打洞。
  - [ ] 会话打洞优先级高于普通后台探测。
  - [ ] RDP、SSH 等交互连接默认低延迟优先。
  - [ ] SMB、文件传输类连接默认吞吐优先。

- [ ] 早期包短暂缓存
  - [ ] 隧道未 ready 时缓存 TCP SYN 和少量早期 TCP 包。
  - [ ] 缓存时间建议 3-10 秒。
  - [ ] 限制每 peer、每会话、全局缓存容量，避免内存失控。
  - [ ] 路径 ready 后立即 replay 早期包。
  - [ ] 路径失败或会话超时后丢弃缓存并记录原因。

## 第三阶段：热路径复用

- [ ] 建立热路径记录
  - [ ] P2P 成功后记录 peer pair、目标端口、最近成功公网地址、最近成功路径、成功时间。
  - [ ] 对 RDP、SSH、SMB 等常用连接保留短期热路径。
  - [ ] 空闲过期时间建议 10-30 分钟。

- [ ] 热路径保活
  - [ ] 热路径空闲期间维持低频 keepalive。
  - [ ] keepalive 频率随空闲时间逐步降低。
  - [ ] 发现路径失效后降级为重新打洞，不误报 ready。

- [ ] 下次连接快速复用
  - [ ] 同一对设备、同一业务端口再次连接时，优先尝试最近成功路径。
  - [ ] 复用热路径的同时并行探测新路径，避免公网端口变化导致卡死。
  - [ ] 热路径失败时快速回到会话感知打洞。

## 第四阶段：持久学习规则

- [ ] 成功连接学习
  - [ ] 同一对设备、同一端口多次成功后，提升为持久学习规则。
  - [ ] 记录成功次数、失败次数、最近成功时间、最近路径质量。
  - [ ] 规则可按端口和设备对维度管理。

- [ ] 启动预热
  - [ ] 客户端启动后对持久学习规则进行低频预热。
  - [ ] 预热不阻塞正常启动。
  - [ ] 预热失败不直接判定设备不可用，只更新路径质量。

- [ ] 管理后台展示
  - [ ] 展示常用连接规则。
  - [ ] 展示最近成功路径、最近失败原因、路径质量。
  - [ ] 提供启用/禁用预热的配置项。

## 第五阶段：路径状态真实化

- [ ] 细化路径状态
  - [ ] `p2p_connecting`：正在 P2P 打洞。
  - [ ] `one_way_p2p`：单边收到探测包，尚未双向确认。
  - [ ] `p2p_ready`：双向确认完成，可承载数据。
  - [ ] `long_punching`：快速打洞失败后持续降频打洞。
  - [ ] `relay_disabled`：服务端禁用 relay。
  - [ ] `relay_ready`：relay 可用并已确认收发。
  - [ ] `unreachable`：P2P 和可用 fallback 均失败。

- [ ] 日志补全
  - [ ] 记录每次路径状态转换。
  - [ ] 记录 ready 判定所需的双向 ack 信息。
  - [ ] 记录会话感知打洞的触发来源，例如 `tcp_syn dst=3389`。
  - [ ] 记录早期包缓存、replay、丢弃原因。

- [ ] 诊断信息补全
  - [ ] 诊断 JSON 输出每个 peer 的当前路径状态。
  - [ ] 输出是否 relay disabled。
  - [ ] 输出活跃会话、热路径、最近失败原因。
  - [ ] 输出 socket rotation 次数和最近原因。

## 验收标准

- [ ] 服务端禁用 relay 时，P2P 失败后不会进入不可用 `relay_udp` 死状态。
- [ ] 单边探测成功不会被标记为 `p2p_datagram ready`。
- [ ] RDP `3389` 连接触发会话感知打洞。
- [ ] 隧道未 ready 时，TCP SYN 不会立即丢弃，而是短暂缓存等待路径 ready。
- [ ] 曾经成功的 RDP/SSH/SMB 路径在短时间内再次连接可以快速复用。
- [ ] socket rotation 明显减少，不再频繁破坏 NAT 映射。
- [ ] 管理后台和诊断日志能明确区分 `one_way_p2p`、`long_punching`、`relay_disabled`、`unreachable`。

## 当前判断

- 当前日志显示 `.2` 和 `.3` 的虚拟网卡与 bootstrap 正常。
- 当前主要问题是 P2P 路径状态分裂：`.2` 单边认为 `.3` 是 `p2p_datagram ready`，`.3` 实际 KCP ready 超时并进入 relay。
- 服务端禁用中继时，`relay_udp` 不是可用 fallback，客户端必须持续 P2P 并真实展示状态。
- 会话感知打洞和热路径复用是接近老 Agent 连接体验的关键方向。
