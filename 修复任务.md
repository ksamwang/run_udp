# UDPTunnelLAN 修复任务清单

本文档作为后续修复 `cmd/UDPTunnelLAN` 产品线的执行参照。完成某项后，把对应的 `- [ ]` 改为 `- [x]`。

## 产品边界确认

- [x] 本轮修复不修改 `cmd/client/**`。
- [x] 本轮修复不改变老 Agent 的运行模式、配置格式、服务名、更新逻辑和端口转发行为。
- [x] 本轮修复不对 `internal/tunnel`、`internal/protocol`、`internal/secure` 做会改变老 Agent 语义的修改。
- [x] LAN 新能力优先放到 `cmd/UDPTunnelLAN/**`、`internal/lan/**`、`internal/packet/**`、`internal/wintun/**`、`internal/vnet/**`，必要时新增 `internal/lantransport/**`。
- [x] 服务端改动限定在 LAN 分支：`/api/lan/*`、`handleLANRegister`、`lanPeers`、LAN packet relay 等。
- [x] 每轮修复后执行 `go test ./cmd/client`，确认老 Agent 没被共享代码误伤。

## P0：先解决大多数场景无法连接

### P0-1 接通 LAN HTTP relay fallback

当前问题：

- `runPacketForwarding` 只启动 Wintun 读包、`sendPackets` 和配置刷新，没有启动 `pollRelayPackets`。
- `sendPackets` 在 `p2p.Send` 失败后直接打印 `LAN tunnel unavailable; drop...` 并丢包，没有调用 `postRelayFrames`。

任务：

- [x] 在 `cmd/UDPTunnelLAN/packet_runtime.go` 中启动 `pollRelayPackets(...)`。
- [x] `sendPackets` 在 P2P 不可用时调用 `postRelayFrames`，不再直接丢包。
- [x] relay 失败时加入短退避，避免刷日志和高频 HTTP 请求。
- [x] relay payload 走 LAN packet 加密，不把原始 IP 包明文发 HTTP relay。
- [x] 增加测试覆盖 P2P 不可用时走 HTTP relay。

### P0-2 增加 LAN 专用 relay 开关

当前问题：

- 服务端 `allow_relay` 是老 Agent UDP relay 的全局开关，默认 `false`。
- LAN HTTP packet relay 复用该语义会影响产品边界。

任务：

- [x] 新增 LAN 专用配置或系统设置，例如 `lan_allow_relay` 或 `lan_packet_relay_enabled`。
- [x] 新开关只影响 `/api/lan/packets/send` 和 `/api/lan/packets/poll`。
- [x] 不改变老 Agent UDP relay 行为。
- [x] 增加测试覆盖 LAN relay 开关开启和关闭。

### P0-3 缩短 P2P fallback 等待

当前问题：

- LAN 客户端打洞失败后等待 `lanPunchTimeout`，目前是 30 秒，用户会长时间感知为不可用。

任务：

- [x] 初始连接快速尝试 P2P 3-5 秒。
- [x] P2P 未就绪时立即启用 HTTP relay。
- [x] P2P 后台继续打洞。
- [x] P2P 打通后自动从 relay 切换到 P2P。
- [x] 增加测试覆盖 relay 到 P2P 的路径切换。

### P0-4 修复服务端 LAN peer 过期和换网问题

当前问题：

- `cleanupLoop` 只清理普通 `a.peers`，没有清理 `a.lanPeers`。
- `handleLANRegister` 没有像普通 `handleRegister` 一样删除旧 socket 对应的 relay route。
- 设备换网、休眠唤醒、公网端口变化后，服务端可能继续返回旧地址。

任务：

- [x] 在 `cleanupLoop` 中同步清理 `a.lanPeers`。
- [x] `handleLANRegister` 检测同一设备同一 peer 换 socket 时删除旧 `pairs`。
- [x] 补充单元测试覆盖 LAN peer TTL。
- [x] 补充单元测试覆盖 LAN peer 换 socket。

## P1：修复连上但不可用的问题

### P1-5 修复 CIDR 和虚拟 IP 不一致

当前问题：

- 客户端发现服务端网络 CIDR 冲突后会本地选择下一个 CIDR。
- 但服务端分配的虚拟 IP 仍属于原网络，可能出现虚拟 IP 和本地路由 CIDR 不一致。

任务：

- [x] 短期内禁止客户端私自换 CIDR 后继续运行。
- [x] 如果服务端分配的 CIDR 和本机路由冲突，runtime 状态设为 `error` 或 `needs_admin_action`。
- [x] 上报 `route_conflict`、`selected_cidr`、`last_error`。
- [x] 提示管理员在控制面修改 LAN 网络 CIDR 后重新分配地址。
- [x] 增加测试覆盖路由冲突时不继续启动数据面。

### P1-6 隧道未就绪时增加短队列

当前问题：

- Wintun 一启动就读包，但 P2P/KCP/relay 还没 ready 时，`sendPackets` 直接丢包。
- 用户刚启动服务就发起 ping 或 RDP 时，第一批连接请求容易失败。

任务：

- [x] 在 LAN 发送路径增加短暂队列。
- [x] 对 TCP SYN、ICMP echo 等启动期关键包保留 2-5 秒。
- [x] 隧道 ready 后重放短队列。
- [x] 队列设置数量和字节上限，防止内存膨胀。
- [x] 增加测试覆盖隧道 ready 前后的队列重放。

### P1-7 支持 UDP 单播包转发

当前问题：

- `internal/packet/router.go` 当前只允许 TCP 和 ICMP，UDP 会返回 `ErrUnsupportedProtocol`。
- 这会影响 DNS、部分业务软件和局域网体验。

任务：

- [x] 在 LAN router 中允许 IPv4 UDP 单播路由。
- [x] 复用现有 ACL 的 `udp` 协议匹配。
- [x] 暂不扩展广播、组播、mDNS，第一阶段只支持单播 UDP。
- [x] 增加测试覆盖 UDP 单播允许、拒绝和 ACL 匹配。

## P2：改善延迟和吞吐

### P2-8 新建 LAN 专用 datagram transport

当前问题：

- 当前所有虚拟网卡 IP 包都经 `writeLANFrame` 塞进单条可靠 KCP 字节流。
- 内层 TCP 已有重传和拥塞控制，外层 KCP 再可靠有序传输会造成 head-of-line blocking、重复重传、延迟抖动和吞吐下降。

任务：

- [x] 不修改老 Agent 使用的 `internal/tunnel`。
- [x] 新增 LAN 专用 transport，例如 `internal/lantransport`。
- [x] 面向 IP packet 做 UDP datagram 转发。
- [x] 每个 IP 包独立加密、独立发送。
- [x] 避免所有流量阻塞在单条可靠字节流上。
- [x] 增加测试覆盖 datagram 加密、解密、乱序和丢包场景。

### P2-9 保留 KCP 作为兼容或最后 fallback

任务：

- [x] 默认优先 P2P UDP datagram。
- [x] 第二优先 HTTP relay encrypted frame。
- [x] KCP stream 作为兼容路径或最后 fallback。
- [x] LAN 数据面不再把 KCP 作为唯一承载方式。
- [x] 增加测试覆盖 transport 优先级选择。

### P2-10 降低人为延迟

当前问题：

- `sendPackets` 默认等 20ms 才 flush。
- Wintun 空读后存在额外 sleep。
- HTTP relay 使用 JSON/base64 会增加额外开销。

任务：

- [x] 低流量立即 flush，高流量再批量。
- [x] relay poll 使用长轮询，避免固定 sleep 叠加。
- [x] Wintun 读包尽量依赖 wait event，避免额外长 sleep。
- [x] 评估二进制 relay frame，减少 JSON/base64 开销。
- [x] 增加基准或集成测试记录延迟和吞吐变化。

## 验证清单

每轮修复至少执行：

- [x] `go test ./cmd/UDPTunnelLAN ./internal/packet ./internal/lan ./internal/wintun ./internal/server`
- [x] `go test ./cmd/client`

端到端场景验证：

- [ ] 同 LAN P2P。
- [ ] 两端 NAT。
- [ ] 一端对称 NAT 或 CGNAT。
- [ ] 服务端 LAN relay 开关关闭。
- [ ] 服务端 LAN relay 开关开启。
- [ ] 本机路由与虚拟网络 CIDR 冲突。
- [ ] 休眠唤醒。
- [ ] 切换 Wi-Fi 或公网端口变化。
- [ ] `ping`。
- [ ] RDP。
- [ ] TCP 吞吐测试。
- [ ] UDP 单播测试。

## 推荐落地顺序

- [x] P0-1：接通 LAN HTTP relay fallback。
- [x] P0-2：增加 LAN 专用 relay 开关。
- [x] P0-4：修复 LAN peer 清理和换 socket。
- [x] P0-3：缩短 P2P fallback 等待，并支持 relay 与 P2P 自动切换。
- [x] P1-5：修复 CIDR 与虚拟 IP 不一致。
- [x] P1-6：增加启动期短队列。
- [x] P1-7：支持 UDP 单播。
- [x] P2-8 到 P2-10：重构 LAN 专用 datagram transport 和性能路径。
