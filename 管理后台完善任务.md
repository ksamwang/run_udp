# 管理后台完善任务

本清单用于对照 Agent / UDPTunnelLAN 当前能力，补齐管理后台未实现或不完整的功能。已完成后把对应项改为 `[x]`。

## P0 必补

### P0-1 补齐 UDPTunnelLAN relay 开关

当前问题：

- 后端 `/api/admin/settings` 已支持 `lan_allow_relay`。
- UDPTunnelLAN 的 HTTP relay fallback 依赖 `lan_allow_relay`。
- 前端 `Settings` 类型和设置页没有该字段，后台无法查看或修改 LAN relay 开关。

任务：

- [ ] 在 `frontend-admin/src/types/api.ts` 的 `Settings` 中加入 `lan_allow_relay`。
- [ ] 在 `frontend-admin/src/pages/SettingsPage.tsx` 增加 LAN relay 开关。
- [ ] 保存设置时带上 `lan_allow_relay`。
- [ ] 高风险确认逻辑覆盖关闭/开启 LAN relay 的影响提示。
- [ ] 验证 `/api/admin/settings` GET/PATCH 与页面状态一致。

### P0-2 增加 UDPTunnelLAN 虚拟路由管理

当前问题：

- 数据模型已有 `VirtualRoute`。
- store 已有 `UpsertVirtualRoute`、`ListVirtualRoutes`。
- LAN bootstrap 已下发 `routes`。
- 后台没有虚拟路由 admin API，也没有前端管理入口。

任务：

- [ ] 增加 `/api/admin/lan/routes` 列表接口，支持按 `network_id`、`device_id` 过滤。
- [ ] 增加虚拟路由新增/更新接口。
- [ ] 增加虚拟路由删除接口。
- [ ] 前端增加 `VirtualRoute` 类型和 `api/lan.ts` 调用。
- [ ] `LanPage` 增加“虚拟路由”页签。
- [ ] 路由表单支持选择设备、填写 CIDR、设置 `advertise`、`accept`。
- [ ] 校验 CIDR 格式和设备是否存在。
- [ ] 增加后端测试覆盖路由 CRUD 和 bootstrap routes 下发。

### P0-3 完整支持多虚拟网络管理

当前问题：

- 后端已有虚拟网络创建、更新、删除能力。
- 前端只开放默认网络更新，没有创建、删除、切换多个网络的完整体验。

任务：

- [ ] 在 `frontend-admin/src/api/lan.ts` 增加创建/删除虚拟网络 API。
- [ ] `LanPage` 支持选择当前网络。
- [ ] 增加创建虚拟网络入口。
- [ ] 增加删除虚拟网络入口和风险确认。
- [ ] 地址、ACL、Peer 状态、路由页签按当前网络过滤。
- [ ] 处理删除网络时地址、ACL、路由、状态的联动风险提示。

## P1 应补

### P1-1 展示 UDPTunnelLAN 虚拟设备密钥状态

当前问题：

- UDPTunnelLAN bootstrap 会上传 public key。
- 服务端保存到 `virtual_device_keys`，并下发给 peer。
- 后台无法查看 key 是否存在、算法、更新时间。

任务：

- [ ] 增加虚拟设备密钥列表 admin API。
- [ ] 前端展示每台 LAN 设备的 key 状态。
- [ ] 展示 algorithm、public key 摘要、updated_at。
- [ ] 在 key 缺失时给出诊断提示。

### P1-2 完善 LAN 地址管理

当前问题：

- LAN 地址主要由 bootstrap 自动分配。
- 后台只能编辑已有地址，不能预分配、释放、重新分配。
- 后端校验只检查 IP 格式，未校验是否属于网络 CIDR。

任务：

- [ ] 支持给已存在设备预分配虚拟 IP。
- [ ] 支持释放设备虚拟 IP。
- [ ] 支持一键重新分配虚拟 IP。
- [ ] 校验虚拟 IP 必须落在对应网络 CIDR 内。
- [ ] 校验虚拟 IP 不能是网络地址或广播地址。
- [ ] 前端展示地址冲突和 CIDR 越界错误。

### P1-3 增加 LAN 单设备聚合状态

当前问题：

- 当前后台只展示 `device_id + peer_id + network_id` 粒度的 peer state。
- 缺少按设备聚合的虚拟网卡、虚拟 IP、路由冲突、路径分布和最近上报时间。

任务：

- [ ] 后端增加 LAN 设备状态聚合视图或由前端聚合。
- [ ] 展示设备虚拟 IP、adapter_state、selected_cidr、route_conflict。
- [ ] 展示 P2P/relay/down peer 数量。
- [ ] 展示最近 bootstrap/status 更新时间。
- [ ] 支持按设备快速过滤 peer state。

### P1-4 增强规则与隧道诊断视图

当前问题：

- Agent 已上报 tunnel state、NAT 类型、失败原因、重试信息。
- 后台规则页只展示基础运行状态，缺少按规则关联的深度诊断。

任务：

- [ ] 规则详情展示关联 tunnel state。
- [ ] 展示 NAT 类型、via、RTT、attempt、next_retry_at。
- [ ] 展示最近错误和最后状态变更时间。
- [ ] 支持从规则跳转到相关会话和隧道状态。

## P2 可增强

### P2-1 完善客户端发布管理

当前问题：

- Agent 已支持 `/api/client/release` 拉取发布信息、下载安装包并校验 SHA256。
- 后台只支持手动填写发布字段。
- 没有安装包上传、自动计算 SHA256、下载 URL 可用性校验。

任务：

- [ ] 增加安装包上传能力。
- [ ] 上传后自动计算 SHA256。
- [ ] 自动填充 `client_release_file` 或 `client_release_url`。
- [ ] 保存前校验下载 URL 可访问。
- [ ] 展示当前发布版本与最低支持版本。

### P2-2 增加审计日志页面

当前问题：

- 后端已有 `audit_events`，部分管理操作已写审计。
- 后台没有审计日志查看页。

任务：

- [ ] 增加 audit list admin API。
- [ ] 前端增加审计日志页面。
- [ ] 支持按 kind、时间、关键字过滤。
- [ ] 展示操作类型、详情、时间。

### P2-3 增强管理员账号管理

当前问题：

- 当前后台只支持当前管理员修改密码。
- 没有管理员列表、新增/禁用、角色、登录会话管理。

任务：

- [ ] 增加管理员列表。
- [ ] 支持新增管理员。
- [ ] 支持禁用管理员或重置密码。
- [ ] 支持查看和撤销 refresh token。
- [ ] 明确角色权限模型。

## 当前未决问题

- [ ] ACL 的 `source_group_id` / `target_group_id` 是否要正式做设备组/标签能力，还是先隐藏并只保留点对点 ACL。
- [ ] 多虚拟网络是否需要 UDPTunnelLAN 客户端支持选择网络，还是继续由服务端默认网络决定。
- [ ] 虚拟路由的实际产品语义：只支持子网宣告，还是还要支持出口网关、默认路由、DNS 路由。
- [ ] LAN 地址释放后，在线设备下一次 bootstrap 是否自动重新分配，是否需要后台提供“踢设备重新 bootstrap”能力。
- [ ] 客户端发布管理是否只服务老 Agent，还是也要覆盖 UDPTunnelLAN 安装包和版本更新。
- [ ] 审计日志是否需要记录操作者、IP、User-Agent；当前模型只有 kind/detail/created_at。
- [ ] 管理员角色是否只区分 admin/viewer，还是需要按设备、规则、LAN 网络做细粒度权限。
