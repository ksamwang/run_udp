# 管理后台拆分任务

目标：把管理后台里的统一设备身份、老 Agent 转发能力、UDPTunnelLAN 虚拟局域网能力拆清楚，避免 Agent 和 UDPTunnelLAN 混在同一套在线状态、规则选择和运行视图里。

## 总体原则

- [ ] 设备身份统一：同一台机器仍然使用同一个 `device_id`。
- [ ] 产品能力分离：Agent 和 UDPTunnelLAN 是同一设备下的不同产品能力。
- [ ] 在线状态分离：不能继续用单一 `online` 表示所有产品在线。
- [ ] 业务页面分离：转发规则只服务 Agent，虚拟局域网只服务 UDPTunnelLAN。
- [ ] 不改老 Agent 客户端边界：优先通过服务端聚合和管理后台展示调整实现。
- [ ] 新增 `device_product_states` 作为 Agent / UDPTunnelLAN 产品状态事实表。
- [ ] 旧 `devices.online` 字段保留兼容，但设备页不再以它作为唯一在线状态。

## 第一阶段：后台命名和边界明确

- [x] 将菜单 `转发规则` 改为 `Agent 转发规则`。
- [x] 将转发规则页面标题改为 `Agent 转发规则`。
- [x] 在转发规则页面增加明确范围提示：该功能只适用于老 Agent 客户端。
- [x] 保持 `虚拟局域网` 页面作为 UDPTunnelLAN 专属管理区。
- [x] 设备页定位调整为统一设备资产总览，而不是某个产品的运行页。

## 第二阶段：设备页拆出产品能力

- [x] 设备列表增加产品能力列：
  - [x] `Agent`
  - [x] `UDPTunnelLAN`
  - [x] `Agent + LAN`
- [x] 设备列表增加 Agent 状态：
  - [x] `agent_online`
  - [x] `last_agent_seen`
- [x] 设备列表增加 LAN 状态：
  - [x] `lan_online`
  - [x] `last_lan_seen`
- [x] 设备列表增加最近来源：
  - [x] `agent_register`
  - [x] `agent_heartbeat`
  - [x] `lan_bootstrap`
  - [x] `lan_status`
- [x] 设备详情按产品分区：
  - [x] 基础信息
  - [x] Agent
  - [x] UDPTunnelLAN
  - [x] 日志 / 诊断
- [x] 基础信息分区展示字段：
  - [x] 设备 ID
  - [x] 设备名
  - [x] 启用状态
  - [x] 产品能力
  - [x] Agent 在线状态
  - [x] LAN 在线状态
  - [x] 最近 Agent 上报时间
  - [x] 最近 LAN 上报时间
- [x] Agent 分区展示字段：
  - [x] Agent 在线状态
  - [x] 最近来源：`agent_register` / `agent_heartbeat`
  - [x] 公网地址
  - [x] UPnP 地址
  - [x] 当前 want / peer
  - [x] 关联转发规则数量
  - [x] Agent 隧道状态摘要
  - [x] Agent 最近错误
- [x] UDPTunnelLAN 分区展示字段：
  - [x] LAN 在线状态
  - [x] 最近来源：`lan_bootstrap` / `lan_status`
  - [x] 虚拟 IP
  - [x] 虚拟网络
  - [x] 虚拟网卡状态
  - [x] 选择网段
  - [x] 路由冲突
  - [x] P2P / Relay / Down 路径摘要
  - [x] 活跃会话数
  - [x] 热路径数
  - [x] Socket rotation 次数和最近原因
  - [x] LAN 最近错误
- [x] Agent 未发现时显示 `未发现 Agent`。
- [x] UDPTunnelLAN 未发现时显示 `未发现 UDPTunnelLAN`。
- [x] 设备页完全改为 Agent / LAN 两个状态，不保留单一总状态作为主展示。
- [x] 不需要展示 Agent 和 LAN 的版本号。

## 第三阶段：Agent 转发规则只允许 Agent 设备

- [ ] 规则创建页面的源设备下拉只显示具备 Agent 能力的设备。
- [ ] 规则创建页面的目标设备下拉只显示具备 Agent 能力的设备。
- [ ] 规则编辑页面同样限制源 / 目标设备。
- [ ] 后端创建规则时校验 `source_id` 必须是 Agent 设备。
- [ ] 后端创建规则时校验 `target_id` 必须是 Agent 设备。
- [ ] 后端更新规则时校验 `source_id` / `target_id` 必须是 Agent 设备。
- [ ] 只有 UDPTunnelLAN 能力的设备不能用于 Agent 转发规则。
- [ ] 规则页设备选择文案说明只显示 Agent 设备。
- [ ] Agent 能力判断标准：有 Agent register / heartbeat 记录即可。
- [ ] Agent 在线超时时间使用现有 TTL。

## 第四阶段：虚拟局域网设备虚拟 IP 表升级

- [ ] `设备虚拟 IP` 表保留地址分配和操作能力。
- [ ] 增加 LAN 在线状态列：
  - [ ] 在线
  - [ ] 离线
  - [ ] 未上报
  - [ ] 异常
- [ ] 增加虚拟网卡状态列：
  - [ ] `up`
  - [ ] `error`
  - [ ] `-`
- [ ] 增加路径概况列：
  - [ ] `P2P / Relay / Down`
- [ ] 增加最近状态时间列：
  - [ ] `last_status_at`
- [ ] 增加最近错误列。
- [ ] 增加路由冲突列。
- [ ] LAN 在线状态只使用 UDPTunnelLAN 的 `lan_bootstrap` / `lan_status` 判断，不使用 Agent 在线状态。
- [ ] LAN 能力判断标准：必须有 UDPTunnelLAN `lan_status` 上报。
- [ ] LAN 在线超时时间固定为 60 秒。

## 第五阶段：后端状态模型调整

- [ ] 短期保留 `devices` 作为统一设备身份表。
- [ ] 新增 `device_product_states` 表。
- [ ] Agent 状态字段：
  - [ ] `agent_seen_at`
  - [ ] `agent_online`
  - [ ] `agent_last_source`
- [ ] LAN 状态字段：
  - [ ] `lan_seen_at`
  - [ ] `lan_online`
  - [ ] `lan_last_source`
- [ ] 产品能力字段：
  - [ ] `product_capabilities`
- [ ] Agent register / heartbeat 只更新 Agent 状态。
- [ ] UDPTunnelLAN bootstrap / status 只更新 LAN 状态。
- [ ] UDPTunnelLAN `lan_bootstrap` 可以更新 `last_source=lan_bootstrap` 用于展示。
- [ ] UDPTunnelLAN `lan_bootstrap` 不作为 LAN 能力判定依据。
- [ ] 统一设备页只做聚合展示，不让某个产品覆盖另一个产品的在线状态。
- [ ] `devices.online` 保留兼容写入，不作为新后台主状态来源。

## 第六阶段：`device_product_states` 表结构

- [ ] 表结构字段：
  - [ ] `device_id`
  - [ ] `product`
  - [ ] `online`
  - [ ] `last_seen_at`
  - [ ] `last_source`
  - [ ] `version`
  - [ ] `last_error`
  - [ ] `metadata`
- [ ] `last_error` 按产品覆盖保存，不保留错误历史。
- [ ] `metadata` 先按产品约定字段写入，不做强 JSON schema 限制。
- [ ] Agent `metadata` 建议字段：
  - [ ] `addr`
  - [ ] `upnp_addr`
  - [ ] `want`
  - [ ] `tunnel_state`
  - [ ] `tunnel_via`
  - [ ] `nat_type`
  - [ ] `health_summary`
  - [ ] `rule_count`
- [ ] UDPTunnelLAN `metadata` 建议字段：
  - [ ] `network_id`
  - [ ] `virtual_ip`
  - [ ] `adapter_state`
  - [ ] `selected_cidr`
  - [ ] `route_conflict`
  - [ ] `p2p_peers`
  - [ ] `relay_peers`
  - [ ] `down_peers`
  - [ ] `active_sessions`
  - [ ] `hot_paths`
  - [ ] `socket_rotations`
  - [ ] `last_rotation_reason`
  - [ ] `relay_disabled`
- [ ] 将 Agent 状态写入 `product=agent`。
- [ ] 将 UDPTunnelLAN 状态写入 `product=lan`。
- [ ] 设备页从该表聚合产品能力和在线状态。
- [ ] Agent 产品能力来源：Agent register / heartbeat。
- [ ] LAN 产品能力来源：UDPTunnelLAN `lan_status`。

## 待确认问题

- [x] 已确认：`metadata` 先按产品约定字段写入。
- [x] 已确认：不考虑新增产品错误历史表。
